"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const core_1 = require("@tsed/core");
const operation_1 = require("./operation");
/**
 * Add security metadata on the decorated method.
 *
 * ## Examples
 * ### On method
 *
 * ```typescript
 * @Controller("/")
 * class ModelCtrl {
 *    @Security("write:calendars")
 *    async method() {}
 * }
 * ```
 *
 * @param {string} securityDefinitionName
 * @param {string} scopes
 * @returns {(...args: any[]) => any}
 * @decorator
 * @swagger
 */
function Security(securityDefinitionName, ...scopes) {
    return (...args) => {
        const type = core_1.getDecoratorType(args);
        switch (type) {
            case "method":
                return operation_1.Operation({ security: [{ [securityDefinitionName]: scopes }] })(...args);
            default:
                throw new Error("Security is only supported on method");
        }
    };
}
exports.Security = Security;

//# sourceMappingURL=security.js.map
