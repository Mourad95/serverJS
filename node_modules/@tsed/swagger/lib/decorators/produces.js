"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const core_1 = require("@tsed/core");
const operation_1 = require("./operation");
/**
 * Add produces metadata on the decorated element.
 *
 * ## Examples
 * ### On method
 *
 * ```typescript
 * class Model {
 *    @Produces("text/html")
 *    id: string;
 * }
 * ```
 *
 * @returns {Function}
 * @decorator
 * @swagger
 * @param produces
 */
function Produces(...produces) {
    return (...args) => {
        const type = core_1.getDecoratorType(args);
        switch (type) {
            case "method":
                return operation_1.Operation({ produces })(...args);
            default:
                throw new Error("Produces is only supported on method");
        }
    };
}
exports.Produces = Produces;

//# sourceMappingURL=produces.js.map
