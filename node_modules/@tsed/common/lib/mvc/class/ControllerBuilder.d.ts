import { IRouterSettings } from "../../config/interfaces/IServerSettings";
import { InjectorService } from "../../di/services/InjectorService";
import { ControllerProvider } from "./ControllerProvider";
export declare class ControllerBuilder {
    private provider;
    private defaultRoutersOptions;
    constructor(provider: ControllerProvider, defaultRoutersOptions?: IRouterSettings);
    /**
     *
     * @returns {any}
     */
    build(injector: InjectorService): this;
    private buildMiddlewares(injector, middlewares);
}
