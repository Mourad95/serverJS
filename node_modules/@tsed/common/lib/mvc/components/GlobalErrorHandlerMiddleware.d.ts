/// <reference types="express" />
import { ServerSettingsService } from "@tsed/common";
import * as Express from "express";
import { IMiddlewareError } from "../interfaces";
import { IResponseError } from "../interfaces/IResponseError";
/**
 * @middleware
 */
export declare class GlobalErrorHandlerMiddleware implements IMiddlewareError {
    private headerName;
    constructor(settingsServerService: ServerSettingsService);
    use(error: any, request: Express.Request, response: Express.Response): any;
    /**
     *
     * @param {e.Response} response
     * @param args
     */
    setHeaders(response: Express.Response, ...args: IResponseError[]): void;
}
