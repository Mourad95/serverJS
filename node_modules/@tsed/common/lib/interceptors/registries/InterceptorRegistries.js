"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const Provider_1 = require("../../di/class/Provider");
const interfaces_1 = require("../../di/interfaces");
const ProviderRegistry_1 = require("../../di/registries/ProviderRegistry");
/**
 *
 * @type {Registry<Provider<any>, IProvider<any>>}
 */
// tslint:disable-next-line: variable-name
exports.InterceptorRegistry = ProviderRegistry_1.GlobalProviders.createRegistry(interfaces_1.ProviderType.INTERCEPTOR, Provider_1.Provider, {
    injectable: true,
    buildable: true
});
/**
 * Add a new interceptor in the `ProviderRegistry`. This interceptor will be built when `InjectorService` will be loaded.
 *
 * #### Example
 *
 * ```typescript
 * import {registerInterceptor, InjectorService} from "@tsed/common";
 *
 * export default class MyInterceptor {
 *     constructor(){}
 *     aroundInvoke() {
 *         return "test";
 *     }
 * }
 *
 * registerInterceptor({provide: MyInterceptor});
 * // or
 * registerInterceptor(MyInterceptor);
 *
 * const injector = new InjectorService()
 * injector.load();
 *
 * const myInterceptor = injector.get<MyInterceptor>(MyInterceptor);
 * myInterceptor.aroundInvoke(); // test
 * ```
 *
 * @param provider Provider configuration.
 */
exports.registerInterceptor = ProviderRegistry_1.GlobalProviders.createRegisterFn(interfaces_1.ProviderType.INTERCEPTOR);

//# sourceMappingURL=InterceptorRegistries.js.map
